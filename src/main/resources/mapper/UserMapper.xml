<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.daitso.mapper.UserMapper">
    <insert id="save" parameterType="com.flab.daitso.dto.user.UserRegister" keyProperty="id">
        INSERT INTO user(userId, userPassword, name, phoneNumber, role, registrationDate)
        VALUES (#{userId}, #{userPassword}, #{name}, #{phoneNumber}, #{role}, #{registrationDate})
    </insert>

    <select id="findByUserId" resultType="com.flab.daitso.dto.user.User">
        SELECT *
        FROM user
        WHERE userId = #{userId}
    </select>

    <select id="findByUserIdAndUserPassword" resultType="com.flab.daitso.dto.user.User">
        SELECT *
        FROM user
        WHERE userId = #{userId}
        AND userPassword = #{userPassword}
    </select>
</mapper>
